{
	"objects" : [
		"acast",
		"add_margins",
		"colsplit",
		"dcast",
		"french_fries",
		"melt",
		"recast",
		"smiths",
		"tips"
	],
	"methods" : {
		"acast" : "acast(data, formula, fun.aggregate = NULL, ..., margins = NULL, subset = NULL, fill = NULL, drop = TRUE, value.var = guess_value(data)) ",
		"add_margins" : "add_margins(df, vars, margins = TRUE) ",
		"colsplit" : "colsplit(string, pattern, names) ",
		"dcast" : "dcast(data, formula, fun.aggregate = NULL, ..., margins = NULL, subset = NULL, fill = NULL, drop = TRUE, value.var = guess_value(data)) ",
		"melt" : "melt(data, ..., na.rm = FALSE, value.name = \"value\") ",
		"recast" : "recast(data, formula, ..., id.var, measure.var) "
	}
}