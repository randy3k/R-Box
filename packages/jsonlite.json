{
  "objects": ["flatten", "fromJSON", "minify", "prettify", "rbind.pages", "serializeJSON", "stream_in", "stream_out", "toJSON", "unbox", "unserializeJSON", "validate"],
  "methods": {
    "flatten": ["flatten(x, recursive = TRUE) "],
    "fromJSON": ["fromJSON(txt, simplifyVector = TRUE, simplifyDataFrame = simplifyVector, simplifyMatrix = simplifyVector, flatten = FALSE, ...) "],
    "minify": ["minify(txt) "],
    "prettify": ["prettify(txt, indent = 4) "],
    "rbind.pages": ["rbind.pages(pages) "],
    "serializeJSON": ["serializeJSON(x, digits = 8, pretty = FALSE) "],
    "stream_in": ["stream_in(con, handler = NULL, pagesize = 500, verbose = TRUE, ...) "],
    "stream_out": ["stream_out(x, con = stdout(), pagesize = 500, verbose = TRUE, ...) "],
    "toJSON": ["toJSON(x, dataframe = c(\"rows\", \"columns\", \"values\"), matrix = c(\"rowmajor\", \"columnmajor\"), Date = c(\"ISO8601\", \"epoch\"), POSIXt = c(\"string\", \"ISO8601\", \"epoch\", \"mongo\"), factor = c(\"string\", \"integer\"), complex = c(\"string\", \"list\"), raw = c(\"base64\", \"hex\", \"mongo\"), null = c(\"list\", \"null\"), na = c(\"null\", \"string\"), auto_unbox = FALSE, digits = 4, pretty = FALSE, force = FALSE, ...) "],
    "unbox": ["unbox(x) "],
    "unserializeJSON": ["unserializeJSON(txt) "],
    "validate": ["validate(txt) "]
  }
}